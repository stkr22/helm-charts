{{- if .Values.ttsEngine.ingress.enabled -}}
{{- $fullName := include "private-assistant.fullname" . -}}
{{- if and .Values.ttsEngine.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.ttsEngine.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.ttsEngine.ingress.annotations "kubernetes.io/ingress.class" .Values.ttsEngine.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}-tts
  labels:
    {{- include "private-assistant.labels" . | nindent 4 }}
  {{- with .Values.ttsEngine.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.ttsEngine.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.ttsEngine.ingress.className }}
  {{- end }}
  {{- if .Values.ttsEngine.ingress.tls }}
  tls:
    {{- range .Values.ttsEngine.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      {{- if .secretName }}
      secretName: {{ .secretName }}
      {{- end }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ttsEngine.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-tts
                port:
                  name: http
              {{- else }}
              serviceName: {{ $fullName }}-tts
              servicePort: 80
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}