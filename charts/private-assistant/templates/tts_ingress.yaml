{{- if .Values.ttsEngine.ingress.enabled -}}
{{- include "private-assistant.ingress" (dict "component" "tts-engine" "serviceName" "tts-engine" "nameSuffix" "tts" "config" .Values.ttsEngine.ingress "context" .) }}
{{- end }}
