{{- if .Values.groundStation.ingress.enabled -}}
{{- $fullName := include "private-assistant.fullname" . -}}
{{- if and .Values.groundStation.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.groundStation.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.groundStation.ingress.annotations "kubernetes.io/ingress.class" .Values.groundStation.ingress.className}}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "private-assistant.labels" . | nindent 4 }}
  {{- with .Values.groundStation.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.groundStation.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.groundStation.ingress.className }}
  {{- end }}
  {{- if .Values.groundStation.ingress.tls }}
  tls:
    {{- range .Values.groundStation.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      {{- if .secretName }}
      secretName: {{ .secretName }}
      {{- end }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.groundStation.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}-ground-station
                port:
                  name: http
              {{- else }}
              serviceName: {{ $fullName }}-ground-station
              servicePort: 80
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
